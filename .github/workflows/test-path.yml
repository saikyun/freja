name: test
on: [push]
jobs:
  test:
    runs-on: windows-latest
    steps:
      - run: md bin
        shell: cmd
      - run: echo echo Hello World >> bin/hello.cmd
        shell: cmd
      - run: dir bin
        shell: cmd
      - run: type bin\hello.cmd
        shell: cmd
      - run: hello
        continue-on-error: true
        shell: cmd
      - run: bin/hello
        shell: cmd
      - run: echo ${{ github.workspace }}\bin >> $GITHUB_PATH
        shell: cmd
      - run: echo %PATH%
#      - run: hello
#        shell: cmd
      - run: echo lul >> $GITHUB_PATH && echo %PATH%
        shell: cmd
      - run: echo %PATH%
        shell: cmd
      - run: echo %PATH%
      - run: echo "${{ github.workspace }}\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
      - run: echo $Env:Path
      - run: echo %PATH%
        shell: cmd